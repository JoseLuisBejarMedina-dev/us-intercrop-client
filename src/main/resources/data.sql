DROP TABLE IF EXISTS client_intercorp;
CREATE TABLE client_intercorp(
   id INT AUTO_INCREMENT PRIMARY KEY,
   age INT,
   first_name VARCHAR(100),
   last_name VARCHAR(100),
   date_of_birth DATE 
);
 